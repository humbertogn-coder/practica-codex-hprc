import pandas as pd

data = {
    "parte": ["computadora", "github", "hprc"],
    "estado": ["listo", "pendiente", "pendiente"],
}

df = pd.DataFrame(data)

print("Mi primera practica reusable con Codex")
print(df)